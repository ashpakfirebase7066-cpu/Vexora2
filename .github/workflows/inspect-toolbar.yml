name: Inspect toolbar source
on:
  workflow_dispatch:
jobs:
  inspect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          sed -n '400,560p' app/src/main/java/com/vexora/editor/MainActivity.kt
